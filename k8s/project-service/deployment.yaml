apiVersion: apps/v1
kind: Deployment
metadata:
  name: project-service
  namespace: taskflow
spec:
  replicas: 1
  selector:
    matchLabels: { app: project-service }
  template:
    metadata:
      labels: { app: project-service }
    spec:
      containers:
        - name: project-service
          image: taskflow-project-service:latest
          imagePullPolicy: Never
          ports: [{ containerPort: 5000 }]
          env:
            - name: JWT_SECRET
              valueFrom: { secretKeyRef: { name: taskflow-secrets, key: JWT_SECRET } }
            - name: DATABASE_URL
              valueFrom: { secretKeyRef: { name: taskflow-secrets, key: DATABASE_URL_PROJECTS } }
            - name: ACTIVITY_SERVICE_URL
              valueFrom: { configMapKeyRef: { name: taskflow-config, key: ACTIVITY_SERVICE_URL } }
            - name: USER_SERVICE_URL
              valueFrom: { configMapKeyRef: { name: taskflow-config, key: USER_SERVICE_URL } }
          livenessProbe:
            httpGet: { path: /health, port: 5000 }
            initialDelaySeconds: 10
            periodSeconds: 15
          readinessProbe:
            httpGet: { path: /health, port: 5000 }
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            requests: { memory: "128Mi", cpu: "100m" }
            limits: { memory: "256Mi", cpu: "300m" }
