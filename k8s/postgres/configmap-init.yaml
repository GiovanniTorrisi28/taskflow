apiVersion: v1
kind: ConfigMap
metadata:
  name: postgres-init
  namespace: taskflow
data:
  init.sql: |
    CREATE DATABASE users_db;
    CREATE DATABASE projects_db;
    CREATE DATABASE activities_db;
