apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-service
  namespace: taskflow
spec:
  replicas: 1
  selector:
    matchLabels: { app: user-service }
  template:
    metadata:
      labels: { app: user-service }
    spec:
      containers:
        - name: user-service
          image: giovannitorrisi/taskflow-user-service:latest
          imagePullPolicy: Always
          ports: [{ containerPort: 5000 }]
          env:
            - name: JWT_SECRET
              valueFrom: { secretKeyRef: { name: taskflow-secrets, key: JWT_SECRET } }
            - name: DATABASE_URL
              valueFrom: { secretKeyRef: { name: taskflow-secrets, key: DATABASE_URL_USERS } }
          livenessProbe:
            httpGet: { path: /health, port: 5000 }
            initialDelaySeconds: 10
            periodSeconds: 15
          readinessProbe:
            httpGet: { path: /health, port: 5000 }
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            requests: { memory: "128Mi", cpu: "100m" }
            limits: { memory: "256Mi", cpu: "300m" }
